# Smithery configuration file: https://smithery.ai/docs/build/project-config

runtime: python
startCommand:
  type: stdio
  commandFunction: |
    (config) => ({
      command: 'python3',
      args: ['smu_mcp.py']
    })
