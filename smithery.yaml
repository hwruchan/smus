# Smithery configuration file: https://smithery.ai/docs/build/project-config

runtime: container
dockerfile: Dockerfile
startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    required: []
